import { Story, Meta } from '@storybook/react/types-6-0';
import Summary from '.';

export default {
  title: 'Summary',
  component: Summary
} as Meta;

export const Default: Story = () => <Summary />;
